package increment

import (
	"src/cmd/smgr/models"
)

func Increment(versions models.VersionSlice, incrementType string, targetReleaseType string) (models.Version, error) {
	// TODO: implement
	// 1. find the highest version

	return incrementedVersion, nil
}
